# Day19：接口自动化项目结构

---

# 一、为什么要做结构化

目前你的代码是：

```python
requests.get(...)
```

问题：

```
代码重复
不好维护
不好扩展
```

企业必须：

```
结构化 + 封装
```

---

# 二、标准项目结构

```
project/
│
├── common/
│   └── request_util.py   # 封装请求
│
├── testcases/
│   └── test_login.py     # 测试用例
│
├── data/
│   └── test_data.json    # 测试数据
│
└── pytest.ini
```

---

# 三、封装 requests（核心）

创建：

```
common/request_util.py
```

代码：

```python
import requests

class RequestUtil:

    def send_request(self, method, url, **kwargs):
        response = requests.request(method, url, **kwargs)
        return response
```

---

# 四、测试用例使用封装

```python
from common.request_util import RequestUtil

def test_get_users():
    req = RequestUtil()

    response = req.send_request(
        "GET",
        "https://jsonplaceholder.typicode.com/users"
    )

    assert response.status_code == 200
```

---

# 五、为什么要封装

封装后你可以：

```
统一处理请求
统一加 token
统一加日志
统一处理异常
```

---

# 六、进阶：自动加 token（核心）

```python
class RequestUtil:

    def __init__(self):
        self.token = None

    def send_request(self, method, url, **kwargs):
        headers = kwargs.get("headers", {})

        if self.token:
            headers["Authorization"] = self.token

        kwargs["headers"] = headers

        response = requests.request(method, url, **kwargs)

        return response
```

---

# 七、真实项目核心思想

```
封装请求
统一管理
解耦测试用例
```

---

# 八、今日练习题

请你：

1 写一个 RequestUtil 类  
2 封装 send_request 方法  
3 在 pytest 中调用它请求：

```
https://jsonplaceholder.typicode.com/users
```

并断言状态码 200

---

答：

```python
import requests

class RequestUtil:

    def send_request(self, method, url, **kwargs):
        response = requests.request(method, url, **kwargs)
        return response
```

```
